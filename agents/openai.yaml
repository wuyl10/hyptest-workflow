interface:
  display_name: "Hyptest Workflow"
  short_description: "Hyptest case workflow for riscv-hyp-tests nhv5.1"
  default_prompt: "Use $hyptest-workflow with optional spec_profile=<name> (defaults to the profile registry default) for hyptest test-point to ai_test_cases/manual_test_cases workflow. Use env vars HYPTEST_HOME, HYPTEST_SPIKE_BIN, HYPTEST_LINKNAN_HOME, HYPTEST_DIFFTEST_REF_SO (and optional HYPTEST_TMPDIR, HYPTEST_CROSS_COMPILE) instead of personal absolute paths or bare legacy names. README examples are not a strict input schema."
